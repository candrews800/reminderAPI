{
  "defaults": {
    "pg": {
      "user": "$PG_USER",
      "host": "localhost",
      "database": "$PG_DB",
      "password": "$PG_PW",
      "port": 5432
    },
    "mailgun": {
      "api_key": "$MAILGUN_API_KEY",
      "domain": "$MAILGUN_DOMAIN"
    }
  },
  "development": {
    "serverPort": 3000,
    "sessionSecret": "supersecret"
  }
}